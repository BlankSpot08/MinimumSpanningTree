package minimumSpanningTree;

import graph.weightedGraph.WeightedGraph;

public class Prims {
    public <T extends Comparable<T>> int algolyze(final WeightedGraph<T> weightedGraph) {

        return -1;
    }
}
